- Concept of git, .git file, commit id, gitignore
- Git fork
- clone
- upstream vs forked, set remote repo
- git init
- git branch out
- Delete branch
- checkout to commit file
- git reset
- Head of commit (understanding log)
- Local vs remote repo
- git rebase (interactive) vs merge
- conflict fix
- cherry pick
- Commit amend, commit message
- Force push (when)
- git bisect
- git stash: pop, apply, and clean (LIFO)
- git revert 
- Git push
- Git mirror
- Github
    - PR
    - Pages
    - Git actions
    - Discussion
    - Review request, rerequest
    - Review other's PR
    - Squash (also comes in rebase)
    - merge

- Config: 
    - Ssh keygen : public and private key
    - add ssh

    git config --global user.name 'Name'
    git config --global user.email 'email'
    git add .
    git commit -m "[Robot]Automated report"
    git pull --rebase remote.git ${{ github.head_ref }}
    git push remote.git HEAD:${{ github.head_ref }}
